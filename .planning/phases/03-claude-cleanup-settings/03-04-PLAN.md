---
phase: 03-claude-cleanup-settings
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - CLAUDE-01
  - CLAUDE-02
  - CLAUDE-03
  - CLAUDE-04
  - CLAUDE-05
  - CLAUDE-06
  - KEY-01
  - KEY-02
  - KEY-03
  - KEY-04
  - KEY-05
  - UI-04
  - UI-05

must_haves:
  truths:
    - "All Phase 3 features work together in an integrated flow"
    - "App launches correctly with persisted settings"
    - "Claude cleanup produces _upraveno.txt with summary, cleaned text, and diff"
    - "App works fully without API key"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 integration: settings, API key management, Claude cleanup pipeline, and all UI elements.

Purpose: Verify all Phase 3 requirements work together end-to-end before marking phase complete.

Output: Verified Phase 3 implementation.
</objective>

<execution_context>
@C:\Users\sosno\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sosno\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-cleanup-settings/03-CONTEXT.md
@.planning/phases/03-claude-cleanup-settings/03-01-SUMMARY.md
@.planning/phases/03-claude-cleanup-settings/03-02-SUMMARY.md
@.planning/phases/03-claude-cleanup-settings/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 3: Claude Cleanup + Settings</name>
  <files>none — verification only</files>
  <read_first>
    - .planning/phases/03-claude-cleanup-settings/03-CONTEXT.md
  </read_first>
  <action>
    CHECKPOINT: Present the verification steps below to the user and wait for approval.

    Launch the app: `cd C:/Users/sosno/whisperai && python -m src.whisperai`

    **Test 1 — No API key mode (CLAUDE-05, CLAUDE-06, KEY-03):**
    1. Verify banner appears at top: "Nastavte Claude API klíč pro čištění textu"
    2. Verify "Přepsat + Upravit" button is disabled (gray)
    3. Add an audio file and click "Přepsat" — transcription works normally
    4. Verify _prepis.txt is saved correctly
    5. Dismiss the banner with X — verify it disappears

    **Test 2 — Settings modal (UI-04, UI-05):**
    1. Click gear icon (⚙) in header
    2. Verify modal opens on General tab with: language, output folder, workers, GPU info
    3. Switch to Claude tab: verify model dropdown, API key field (masked), rate limit guide link
    4. Change language to English, click "Save Settings"
    5. Verify all UI strings switch to English without restart
    6. Reopen settings — verify language is still English (persisted)
    7. Change back to Czech, save

    **Test 3 — API key setup (KEY-01, KEY-02, KEY-05):**
    1. Open settings, Claude tab
    2. Enter your Anthropic API key
    3. Click "Save Settings" — verify "Ověřuji..." then "Klíč platný ✓" appears
    4. Close settings — verify banner is gone
    5. Reopen settings — verify key field shows asterisks (stored)
    6. Click "Odstranit API klíč" — confirm — verify key is removed
    7. Close settings — verify banner reappears

    **Test 4 — Claude cleanup pipeline (CLAUDE-01, CLAUDE-02, CLAUDE-03, CLAUDE-04):**
    1. Re-enter API key in settings
    2. Add an audio file
    3. Optionally create a context profile and enter context text
    4. Click "Přepsat + Upravit"
    5. Watch log: transcription progress, then "Claude: čistím text..." message
    6. Verify both _prepis.txt AND _upraveno.txt are created
    7. Open _upraveno.txt — verify it contains: executive summary, cleaned transcript, diff comparison
    8. Verify _prepis.txt is unchanged (CLAUDE-04)

    **Test 5 — Standalone cleanup (D-01):**
    1. After test 4, file should show "hotovo"
    2. Click "Upravit" button — verify Claude runs again on the completed file
    3. Verify _upraveno_2.txt is created (versioned collision handling, D-08)

    **Test 6 — Error handling (D-03):**
    1. Remove API key from settings
    2. Try "Přepsat + Upravit" — verify button is disabled
    3. Hover over it — verify tooltip "Nejdříve nastavte API klíč Claude"

    **Test 7 — Prompt editor (D-22, D-29):**
    1. Click "Upravit prompt ▸" — verify inline editor appears
    2. Verify default prompt is loaded
    3. Check cost estimate label shows a dollar amount
    4. Click "Obnovit výchozí" — verify prompt resets
    5. Click "Skrýt prompt ▼" — verify editor collapses
  </action>
  <verify>
    <automated>cd C:/Users/sosno/whisperai && python -c "from src.whisperai.utils.settings import SettingsStore, get_api_key; from src.whisperai.core.claude_cleaner import estimate_cost_pre_send, PRICING; from src.whisperai.gui.settings_dialog import SettingsDialog; from src.whisperai.gui.transcription_panel import TranscriptionPanel; print('All imports OK'); print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - App launches without errors
    - All 7 test groups pass as confirmed by user
    - _prepis.txt and _upraveno.txt are produced correctly
    - Settings persist between app restarts
    - Language switch works live without restart
  </acceptance_criteria>
  <done>User has approved all Phase 3 features working correctly end-to-end.</done>
</task>

</tasks>

<verification>
All Phase 3 success criteria from ROADMAP.md:
1. "Přepsat + Upravit" produces _prepis.txt + _upraveno.txt
2. Original _prepis.txt never modified by cleanup step
3. First Claude use prompts for API key; stored in OS credential manager
4. User can skip API key and use "Přepsat" without crash
5. Settings panel shows/updates/removes API key
6. Settings persist output folder, language, worker count between sessions
</verification>

<success_criteria>
Human confirms all 7 test groups pass. Phase 3 marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-cleanup-settings/03-04-SUMMARY.md`
</output>
