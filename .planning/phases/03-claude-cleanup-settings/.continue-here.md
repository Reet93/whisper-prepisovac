---
phase: 03-claude-cleanup-settings
task: 03-04 (human verification)
total_tasks: 4 plans (3 done, 1 in progress)
status: in_progress
last_updated: 2026-03-28T20:24:33.418Z
---

<current_state>
Phase 3 execution paused during Wave 3 — plan 03-04 human verification checkpoint.
Plans 03-01 (backend), 03-02 (settings dialog), 03-03 (Claude UI) all complete with summaries.
User started testing, found 2 integration bugs, both fixed but not yet committed.
Tests 3-7 of 7 not yet attempted.
</current_state>

<completed_work>

- Plan 03-01: Settings store + Claude backend — Done (4 commits, 5ac6d88..9bb29c0)
- Plan 03-02: Settings dialog + main window integration — Done (3 commits, 3ab14d7..8031437)
- Plan 03-03: Claude cleanup UI + pipeline — Done (2 commits, ab5bd36..f3a7671)
- Plan 03-04: Human verification — In progress
  - Test 1 (no API key mode): banner not showing (BUG FOUND + FIXED), buttons work, transcription works
  - Test 2 (settings modal): all pass except language live-reload (BUG FOUND + FIXED)
  - Tests 3-7: not yet attempted
</completed_work>

<remaining_work>

- Commit 3 bug fix files (main.py, main_window.py, settings_dialog.py)
- Re-test Test 1 (banner) and Test 2 (language reload) after fixes
- Test 3: API key setup via settings (KEY-01, KEY-02, KEY-05)
- Test 4: Claude cleanup pipeline end-to-end (CLAUDE-01 through CLAUDE-04)
- Test 5: Standalone cleanup / collision handling
- Test 6: Error handling (disabled button, tooltip)
- Test 7: Prompt editor (expand/collapse, reset, cost estimate)
- After all tests pass: verification, roadmap update, phase completion
</remaining_work>

<decisions_made>

- Fixed banner overlap by wrapping TranscriptionPanel in its own frame at row=1 in content_frame (banner stays at row=0)
- Fixed language callback by passing explicit on_language_changed callback to SettingsDialog instead of relying on hasattr check on ttk.Window
- Removed stale `lang` argument from `create_app()` call in main.py — app.py reads language from SettingsStore now
- Installed platformdirs, keyring, anthropic into venv (were in requirements.in but not installed)
</decisions_made>

<blockers>
- None blocking — just need user time to complete manual testing
</blockers>

<context>
The 3 code plans executed cleanly via parallel subagents. Integration bugs are typical for parallel execution where agents don't see each other's changes (main.py signature mismatch, grid row collision, callback wiring). All fixes are straightforward and already applied. The remaining tests (3-7) require the user's Anthropic API key and manual interaction with the app.
</context>

<next_action>
Start with: Commit the 3 bug fixes, then re-launch the app and resume the checkpoint verification from Test 1 (confirm banner now shows).
</next_action>
