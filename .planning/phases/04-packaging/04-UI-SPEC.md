---
phase: 4
slug: packaging
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-29
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: Packaging.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tkinter + ttkbootstrap, not shadcn-eligible) |
| Preset | not applicable |
| Component library | ttkbootstrap 1.20.2 — ttk widget set with bootstyle theming |
| Icon library | Unicode symbols inline (no external icon library) |
| Font | System default via ttkbootstrap; Consolas 9px (Windows) / Menlo 9px (macOS) for monospace log |
| Theme | flatly (confirmed in app.py — `themename="flatly"`) |

**shadcn gate:** Not executed. Project stack is Python/Tkinter, not React/Next.js/Vite.
Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (must be multiples of 4). Pre-populated from existing codebase patterns (main_window.py, settings_dialog.py).

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline button padding (`padx=(4, 0)`) |
| sm | 8px | Compact row spacing, internal grid gaps |
| md | 16px | Default frame padding (`padding=16` — used in main_frame, settings tabs, dialog frames) |
| lg | 24px | Section-level vertical separation |
| xl | 32px | Major layout gaps (not currently used; reserve for future) |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Banner pady: `(0, 4)` — 4px bottom only, to tuck banner flush to header
- Progress dialog interior padding: `padding=16` on outer frame, `pady=8` between rows

---

## Typography

Pre-populated from existing codebase (main_window.py, transcription_panel.py, settings_dialog.py).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 10px (system default via ttkbootstrap) | regular (400) | 1.4 (ttkbootstrap default) |
| Label / caption | 9px (`font=("", 9)`) | regular (400) | 1.3 |
| Heading / title | 13px (`font=("", 13, "bold")`) | bold (700) | 1.2 |
| Sub-heading | 10px (`font=("", 10, "bold")`) | bold (700) | 1.2 |

**Download dialog specific:**
- Dialog title label: 13px bold — matches app title heading
- Progress status label: 9px regular — matches caption style
- Size/time hint label: 9px regular, foreground `#95A5A6` (muted) — matches existing progress count style

**Monospace log (existing pattern, not new in Phase 4):**
- Consolas 9px (Windows) / Menlo 9px (macOS)

---

## Color

Pre-populated from ttkbootstrap `flatly` theme + existing codebase color usage.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` / `#F8F9FA` (flatly surface) | Window background, frame backgrounds |
| Secondary (30%) | `#E9ECEF` (flatly secondary surface) | Cards, sidebar-equivalent panels, notebook tabs |
| Accent (10%) | `#2C3E50` (flatly dark / ttkbootstrap `primary`) | Primary action buttons ("Přepsat", "Download") |
| Destructive | `#E74C3C` (flatly danger — bootstyle="danger") | Destructive buttons only (cancel in-progress download, error state labels) |
| Warning surface | ttkbootstrap `warning` bootstyle | Banner frames (existing pattern) |
| Success | ttkbootstrap `success` bootstyle | Completion states ("Přepsat + Upravit" button, download complete) |
| Muted text | `#95A5A6` | Hint labels, captions, read-only status text |

Accent (`#2C3E50` / bootstyle="primary") reserved for:
- Primary CTA button in download dialog ("Stáhnout model" / "Download Model")
- No other elements — secondary actions use `bootstyle="secondary"`

---

## Copywriting Contract

Phase 4 introduces one new UI surface: the first-launch model download dialog (D-08).
All other UI surfaces are unchanged from Phases 1–3.

### Czech (cs) — primary locale

| Element | Copy |
|---------|------|
| Primary CTA — download dialog | "Stáhnout model" |
| Download dialog title | "Stažení modelu Whisper" |
| Download dialog body | "Model Whisper (medium, ~1,5 GB) se stahuje. Stahování proběhne pouze jednou." |
| Download progress label | "Stahování…" |
| Download size hint | "Přibližná velikost: 1,5 GB. Čas: 2–15 minut dle rychlosti připojení." |
| Download complete label | "Model byl úspěšně stažen." |
| Download error heading | "Stahování selhalo" |
| Download error body | "Nepodařilo se stáhnout model. Zkontrolujte připojení k internetu a zkuste to znovu." |
| Download retry button | "Zkusit znovu" |
| Download cancel button | "Zrušit stahování" |
| Empty state — download cancelled | "Aplikace bez modelu nemůže přepisovat. Model lze stáhnout při příštím spuštění." |

### English (en) — secondary locale

| Element | Copy |
|---------|------|
| Primary CTA — download dialog | "Download Model" |
| Download dialog title | "Download Whisper Model" |
| Download dialog body | "The Whisper model (medium, ~1.5 GB) is being downloaded. This only happens once." |
| Download progress label | "Downloading…" |
| Download size hint | "Approx. size: 1.5 GB. Time: 2–15 min depending on connection speed." |
| Download complete label | "Model downloaded successfully." |
| Download error heading | "Download Failed" |
| Download error body | "Could not download the model. Check your internet connection and try again." |
| Download retry button | "Try Again" |
| Download cancel button | "Cancel Download" |
| Empty state — download cancelled | "The app cannot transcribe without the model. You can download it on next launch." |

### Destructive actions in Phase 4

| Action | Confirmation Approach |
|--------|----------------------|
| Cancel active download | Inline confirmation label: "Zrušit stahování?" with confirm button (`bootstyle="danger"`) + back button (`bootstyle="secondary"`). No separate modal — dialog is already blocking. |

---

## Component Inventory

New components introduced in Phase 4:

### ModelDownloadDialog

**Type:** ttkbootstrap `Toplevel` — blocking modal, shown before main window opens.

**Layout:**
```
┌──────────────────────────────────────────────────┐
│  Stažení modelu Whisper                  [title]  │
│                                                   │
│  Model Whisper (medium, ~1,5 GB) se stahuje.      │
│  Stahování proběhne pouze jednou.        [body]   │
│                                                   │
│  [████████████░░░░░░░░░░░░░░░░]  Indeterminate    │
│                                                   │
│  Stahování…                              [status] │
│  Přibližná velikost: 1,5 GB. Čas: 2–15 min.      │
│                                        [hint]     │
│                                                   │
│                 [Zrušit stahování]                │
└──────────────────────────────────────────────────┘
```

**Progress bar:** `ttk.Progressbar(mode="indeterminate")` — starts automatically. Determinate mode deferred to post-v1 (RESEARCH.md open question 2).

**Cancel flow:** On "Zrušit stahování" click, stop download thread, show inline confirmation row with "Zrušit stahování?" label + confirm/back buttons. On confirm: close dialog, show empty state label in main window log. On back: resume progress display.

**Error state:** Replace progress bar row with error heading (13px bold, `foreground="#E74C3C"`) + error body (9px regular) + "Zkusit znovu" button (`bootstyle="secondary"`) + "Zrušit stahování" button (`bootstyle="danger"`).

**Success state:** Replace progress bar with checkmark label "Model byl úspěšně stažen." (9px, `foreground` success color from flatly theme). Auto-close after 1500ms — consistent with SettingsDialog API key validation success pattern (Phase 3).

**Padding:** outer frame `padding=16`, internal row `pady=8`.

**Minimum size:** 400px wide × 220px tall.

**No parent window shown behind it:** Dialog opens before `root.deiconify()` in main.py. Root window remains withdrawn until download completes or is cancelled.

---

## States and Interactions

### Model Download Dialog States

| State | Trigger | Visual |
|-------|---------|--------|
| Downloading (initial) | App launch, model absent | Indeterminate progress bar + "Stahování…" label |
| Cancel confirmation | User clicks "Zrušit stahování" | Progress pauses, inline "Zrušit stahování?" confirmation row replaces cancel button |
| Error | Network failure / timeout | Progress bar hidden, error heading + body + retry/cancel buttons |
| Success | Download completes | Progress bar hidden, success label shown, auto-close after 1500ms |
| Cancelled | User confirms cancel | Dialog closes, main window opens with warning log line |

### Existing UI (no changes in Phase 4)

All existing states in TranscriptionPanel, MainWindow, and SettingsDialog are unchanged. Phase 4 does not modify these surfaces.

---

## Registry Safety

Not applicable. Project uses Tkinter/ttkbootstrap. No shadcn or third-party component registries.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| None | N/A | N/A — Tkinter/ttkbootstrap project; registry concept does not apply |

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| CONTEXT.md | 12 (D-01 through D-12) — locked distribution decisions |
| RESEARCH.md | 5 (model download pattern, indeterminate bar recommendation, dialog layout, platformdirs path, auto-close pattern) |
| Codebase scan (app.py, main_window.py, settings_dialog.py, transcription_panel.py) | 8 (flatly theme, padding=16, font sizes, bootstyle values, color hex values, auto-close 1500ms, muted #95A5A6, error #E74C3C) |
| User input this session | 0 — all fields resolved from upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
