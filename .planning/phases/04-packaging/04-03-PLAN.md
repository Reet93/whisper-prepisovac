---
phase: 04-packaging
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - .gitignore
  - README.md
  - LICENSE
autonomous: false
requirements: [PKG-02]
user_setup: []

must_haves:
  truths:
    - ".gitignore excludes .planning/, .claude/, .vexp/, models/, .venv/, __pycache__/, dist/, build/, bin/"
    - "README.md describes the project, features, installation, first-launch model download, and usage"
    - "LICENSE file contains MIT license text"
    - "No hardcoded API keys or secrets exist in committed source files"
    - "Built portable folder launches and shows the download dialog or main window"
  artifacts:
    - path: ".gitignore"
      provides: "Git exclusion rules"
      contains: ".planning/"
    - path: "README.md"
      provides: "Project documentation for GitHub"
      contains: "Whisper"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
  key_links: []
---

<objective>
Create Git publication files (.gitignore, README.md, LICENSE), run security scan, clean up unused model file, and verify the built app works.

Purpose: Prepare the repository for GitHub publication (D-01, D-02, D-03, D-04) and verify the portable folder runs correctly. PKG-02 (macOS) is deferred per D-05 but tracked here as acknowledged.
Output: .gitignore, README.md, LICENSE, verified portable folder build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-packaging/04-CONTEXT.md
@.planning/phases/04-packaging/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore, README.md, LICENSE, and run security scan</name>
  <files>.gitignore, README.md, LICENSE</files>
  <read_first>
    - .planning/phases/04-packaging/04-CONTEXT.md (D-01 through D-04, D-09, D-10 decisions)
    - requirements.in (dependency list for README)
    - main.py (entry point for README usage section)
  </read_first>
  <action>
1. Create `.gitignore` with the following content (per D-03):

```
# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Virtual environments
.venv/
.vexp/
venv/

# PyInstaller build artifacts
build/
dist/
*.spec.bak

# Whisper models (downloaded on first launch, ~1.5 GB)
models/

# IDE and editor
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# GSD planning and Claude session artifacts
.planning/
.claude/

# Bundled ffmpeg binaries (downloaded separately for build)
bin/

# Temporary and log files
*.log
*.tmp
```

2. Create `LICENSE` with MIT license text (per D-02). Use the year 2026 and copyright holder as the project. Full MIT license body.

3. Create `README.md` (per D-01, D-09) with the following structure:

```markdown
# Whisper Prepis

Desktop application for transcribing audio recordings to text using OpenAI Whisper, with optional AI-powered text cleanup via the Anthropic Claude API.

Built for Czech audio recordings. Runs locally on Windows without cloud dependency for transcription.

## Features

- **One-click transcription** of audio files (.mp3, .wav, .m4a, .ogg) using Whisper medium model
- **GPU acceleration** — automatically detects and uses NVIDIA CUDA GPU with CPU fallback
- **Batch processing** — load multiple files or entire folders, process in parallel
- **Claude AI cleanup** (optional) — produces edited text with grammar corrections, paragraph structure, executive summary, and diff comparison
- **Bilingual UI** — Czech and English interface
- **Secure API key storage** — uses Windows Credential Manager (never stored in plaintext)
- **Persistent settings** — output folder, language, and worker count saved between sessions

## Requirements

- Windows 10 or 11
- Internet connection for first launch (model download)
- NVIDIA GPU recommended (CUDA-compatible) — CPU works but is slower
- Anthropic API key (optional, for Claude text cleanup features)

## Installation

### For Users (Portable Folder)

1. Download the latest release zip from [Releases](../../releases)
2. Extract the zip to any folder
3. Run `WhisperPrepis.exe`
4. On first launch, the app will download the Whisper model (~1.5 GB). This only happens once.

### For Developers (From Source)

1. Install Python 3.12.x
2. Clone this repository
3. Create a virtual environment and install dependencies:
   ```bash
   python -m venv .venv
   .venv\Scripts\activate
   pip install -r requirements.txt
   ```
4. Run the app:
   ```bash
   python main.py
   ```

## First Launch

On first launch, the app downloads the Whisper medium model (~1.5 GB). This requires an internet connection and takes 2-15 minutes depending on your connection speed. The model is saved locally and reused for all future launches.

## Usage

1. Click **"Pridat soubory"** (Add Files) or **"Pridat slozku"** (Add Folder) to load audio files
2. Click **"Prepsat"** (Transcribe) for raw transcription, or **"Prepsat + Upravit"** for transcription with Claude AI cleanup
3. Output files are saved next to the source audio:
   - `_prepis.txt` — raw transcription
   - `_upraveno.txt` — cleaned-up version with summary and diff (when using Claude)

## Building

To build the portable folder distribution:

1. Install build dependencies:
   ```bash
   .venv\Scripts\pip install pyinstaller==6.19.0
   ```
2. Download ffmpeg static build from [BtbN/FFmpeg-Builds](https://github.com/BtbN/FFmpeg-Builds/releases) and extract `ffmpeg.exe` and `ffprobe.exe` to `bin/`
3. Run the build script:
   ```bash
   build.bat
   ```
4. Output: `dist\WhisperPrepis\` (portable folder) and `dist\WhisperPrepis-v1.0-windows.zip` (release archive)

## Technology

- **Transcription**: faster-whisper (CTranslate2 backend)
- **GPU**: PyTorch with CUDA support
- **AI Cleanup**: Anthropic Claude API
- **GUI**: Tkinter + ttkbootstrap
- **Packaging**: PyInstaller portable folder

## License

MIT License — see [LICENSE](LICENSE) for details.
```

4. Run security scan for hardcoded secrets (per D-04):

```bash
# Scan for potential API keys and secrets
grep -rn "sk-ant-\|sk-proj-\|api_key\s*=\s*['\"]" src/ main.py 2>/dev/null
# Should return no results
```

5. Clean up unused openai-whisper model file (per D-10):
If `models/medium.pt` exists, delete it. The models/ directory is gitignored so this is local cleanup only.

```bash
rm -f models/medium.pt
```

6. Verify no .planning/ or .claude/ files are tracked by git:
```bash
git ls-files .planning/ .claude/ 2>/dev/null
# Should return empty
```
  </action>
  <verify>
    <automated>test -f .gitignore && echo ".gitignore exists" && test -f README.md && echo "README exists" && test -f LICENSE && echo "LICENSE exists" && grep -q ".planning/" .gitignore && echo ".planning excluded" && grep -q "models/" .gitignore && echo "models excluded" && grep -q "MIT" LICENSE && echo "MIT license" && grep -q "first launch" README.md && echo "README has first launch info"</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains `.planning/`
    - .gitignore contains `.claude/`
    - .gitignore contains `.vexp/`
    - .gitignore contains `models/`
    - .gitignore contains `.venv/`
    - .gitignore contains `__pycache__/`
    - .gitignore contains `dist/`
    - .gitignore contains `build/`
    - .gitignore contains `bin/`
    - README.md contains `# Whisper Prepis`
    - README.md contains `~1.5 GB` (first-launch download note per D-09)
    - README.md contains `WhisperPrepis.exe`
    - README.md contains `build.bat`
    - LICENSE contains `MIT License`
    - Security scan returns no results (no hardcoded secrets in src/ or main.py)
  </acceptance_criteria>
  <done>.gitignore created excluding all non-source directories. README.md documents features, installation, first-launch download, usage, and build instructions. MIT LICENSE created. Security scan passed. Unused medium.pt removed if present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify built portable folder runs correctly</name>
  <files>dist/WhisperPrepis/WhisperPrepis.exe</files>
  <action>
This is a human verification checkpoint. The executor presents the verification steps below and waits for user confirmation. No automated action — the user manually tests the built app.
  </action>
  <what-built>Complete Windows portable folder build with model download dialog, ffmpeg bundling, CUDA support, keyring integration, and all app features. Plus GitHub publication files (.gitignore, README.md, LICENSE).</what-built>
  <how-to-verify>
1. Navigate to `dist\WhisperPrepis\` and run `WhisperPrepis.exe`
2. **First-launch check**: If model is not in APPDATA, the download dialog should appear with progress bar and Czech/English text (depending on system language)
3. **After download completes** (or if model already cached): main window should open with the familiar Whisper Prepis interface
4. **Transcription test**: Load a short audio file, click "Prepsat", verify transcription completes and `_prepis.txt` is created
5. **Settings test**: Open settings, verify API key field works (keyring integration), change language, verify it persists
6. **Check dist folder size**: Should be roughly 2-4 GB (CUDA DLLs + torch + CTranslate2). If much larger, openai-whisper may not have been excluded.
7. **Review README.md**: Open in browser/editor, verify formatting and content accuracy

**Expected outcomes:**
- App launches without Python installed (frozen context works)
- Download dialog appears on first launch with no model
- Transcription works with GPU (check log for "cuda" device)
- ffmpeg is found automatically (no "ffmpeg not found" errors)
- Keyring stores/retrieves API key without "No backend" error

**Known limitations:**
- macOS build is deferred (D-05) — PKG-02 is not testable
- Model download requires internet connection
- First build may be large due to CUDA DLLs — this is expected
  </how-to-verify>
  <verify>User confirms app launches and transcription works on built portable folder</verify>
  <done>User has verified: app launches from dist/, download dialog works, transcription completes, settings persist, keyring functions correctly.</done>
  <resume-signal>Type "approved" to confirm the build works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- .gitignore, README.md, LICENSE exist and have correct content
- Built app in dist/WhisperPrepis/ launches and functions correctly
- Model download dialog appears when model is absent
- Transcription works on a test audio file
- No hardcoded secrets in source code
</verification>

<success_criteria>
- Git publication files created and correct
- Security scan passes (no hardcoded secrets)
- Built portable folder launches on Windows without Python installed
- Model download dialog works on first launch
- Transcription completes successfully using bundled ffmpeg and downloaded model
- PKG-02 (macOS) acknowledged as deferred per D-05
</success_criteria>

<output>
After completion, create `.planning/phases/04-packaging/04-03-SUMMARY.md`
</output>
